/**
 * フィーチャーフラグ設定
 * 機能の有効/無効を一元管理
 */
export const FEATURES = {
  // ゲーム機能を一時的に無効化（MVP安定化のため）
  GAME_ENABLED: false,
  
  // 他の機能フラグ（将来的な拡張用）
  CHAT_ENABLED: true,
  VIDEO_CALL_ENABLED: true,
  POMODORO_TIMER_ENABLED: true,
};
